# 073 · 036 sprzatanie po recenzji drobiazgi

**Status:** done
**Kind:** cleanup
**Priority:** 3
**Tier:** standard
**Tags:** zaplecze
**Files:** skills/horde/scripts/tick.mjs, skills/horde/scripts/_lib.mjs, skills/horde/scripts/horde.mjs, skills/horde/scripts/audit.mjs, skills/horde/scripts/status.mjs, skills/horde/scripts/README.md
**Found by:** reviewer 036
**Where:** znaleziska recenzenta issue 036 (Minor, wszystkie akceptowalne bez cofania 036)

## What
Pięć drobiazgów po usunięciu kosztu (036): martwe importy `readJSON`/`writeJSON`/`readText` w tick.mjs (po usunięciu bookCost/waveNumber); `_lib.mjs:966` nadal cytuje nieistniejący placeholder `{{ of limit}}` jako przykład; postrzępione zawijanie linii w kilku edytowanych komentarzach (horde.mjs cmdDone, audit.mjs:50, status.mjs USAGE, trzy miejsca w scripts/README.md); odpowiedź FAQ w README nazywa `config.parallelism` wprost, co idzie wbrew zasadzie "nie pokazuj wewnętrznych nazw czytelnikowi"; dokumenty `horde-plan/1` i `horde-retro/1` zmieniły kształt (cost → weight, cost usunięty) bez podbicia id schematu.

## Why
Nic z tego nie psuje zachowania ani nie blokowało 036, ale zostawione, gromadzi się jako szum: martwy kod, przykład wskazujący na nieistniejącą rzecz, wewnętrzna nazwa w odpowiedzi dla czytelnika.

## Acceptance
Martwe importy usunięte z tick.mjs. `_lib.mjs:966` przykład zaktualizowany albo usunięty. Zawijanie linii poprawione w wymienionych miejscach. FAQ przeformułowane bez nazwy klucza konfiguracji. Rozważyć, czy horde-plan/1 i horde-retro/1 potrzebują nowego id schematu (decyzja może być "nie", jeśli nic poza tym repo tych dokumentów nie czyta).

## Evidence

- **ran:** grep -n readJSON, writeJSON and readText in tick.mjs before editing · **saw:** readJSON and writeJSON matched only the import line, both genuinely dead. readText matched the import line plus a real call at line 192, inside parseEvidenceRows(readText(hordePath(horde, 'charter.md')) or ''). Removed readJSON and writeJSON from the tick.mjs import list; kept readText since it is still used.
- **ran:** grep -rn 'of limit' and 'clean | findings' across the whole repository; git log -S on both strings against skills/horde · **saw:** Neither string appears anywhere outside the _lib.mjs comment itself. Commit b97ef8a (Remove cost tracking entirely) shows '{{ of limit}}' was a real token in templates/mission-close.md and templates/wave-close.md until that commit deleted it. An earlier, unrelated commit (seat cassation, 2c8ed09) had already removed the real '{{clean | findings}}' token from wave-close.md. Replaced both stale examples in the renderTemplate doc comment: kept '{{n-1}}' (still real, wave-close.md), swapped the other two for '{{quality | autonomous}}' and '{{revertBase | }}' (both real today, in charter.md and ticket.md).
- **ran:** diffed commit b97ef8a against HEAD for horde.mjs, audit.mjs, status.mjs and scripts/README.md to find every comment that commit shortened without rewrapping the paragraph · **saw:** found and rewrapped four ragged paragraphs, no wording changed beyond the wrap: horde.mjs's evidence-is-the-plan banner above cmdDone; audit.mjs's what-happens-when-one-cannot-be-read comment (around former line 50); status.mjs's header comment plus its USAGE text (two separate ragged spots in that one file, both fixed); and three README.md paragraphs (the charter Quality-policy bullet, the status.mjs-mirroring digest paragraph, and the wave.mjs close command description) -- matching the issue's count of three README spots exactly.
- **ran:** grep -n parallelism across scripts/*.mjs to see how the real CLI output labels this number, then grep -n config.parallelism in README.md · **saw:** queue.mjs (line 1397, the plan command's own text output) and wave.mjs (lines 184-186, the wave-open journal line) both print the plain word parallelism, never the dotted config.parallelism. In README.md, the tick.mjs walkthrough's dispatch-list step named config.parallelism directly while explaining a behavior (why the dispatch list is capped) to the reader; reworded that clause to say the configured parallelism cap instead. Left config.parallelism named in two spots that genuinely need the literal key: the horde.mjs config get|set key list and the Constants-and-where-they-come-from table. Left two more mechanism-documentation mentions alone too (the plan command's own report-field description and wave.mjs start's journal-bullet description), since both explain where a specific recorded number is sourced from rather than answer a reader's behavior question.
- **ran:** grep -rn horde-plan, horde-retro and RETRO_SCHEMA across scripts/*.mjs and tests/*.mjs; grep -rn the same across reference/*.md and SKILL.md; read queue.mjs buildPlan, retro.mjs cmdRetro, horde.mjs cmdDone and readArchivedMission · **saw:** horde-plan/1 (queue.mjs buildPlan) has no reader anywhere in this codebase -- it is only ever written to stdout via --json, never parsed back by any tool. Its only shape change was renaming the cost field to weight, same two sub-fields (estimate, runsPerTicket), same formula (weightOf(class) times 2, summed) -- identical number, new label. horde-retro/1 has two internal readers: horde.mjs cmdDone compares retro.schema against the RETRO_SCHEMA constant that retro.mjs itself exports (self-referential, always shipped in lockstep as one plugin release, never reads retro.cost), and readArchivedMission (feeding horde.mjs history and refine.mjs's consult scoping) reads archived retro.json files from past, possibly older-version missions without checking schema at all, extracting only law, inexpressible and taste through null-safe helpers -- pre-036 and post-036 shaped documents read identically, no special-casing needed. Neither reference/model.md nor SKILL.md mentions horde-plan or horde-retro anywhere, and there is no formal schema file in the repository, so no documented external contract exists either. Decision: no schema bump for either document -- nothing inside this repository is confused by the shape change, and there is no evidence of any external reader.
- **ran:** cd skills/horde/scripts; HORDE_TEST_YG set to the local Yggdrasil build; node --test tests/tick.test.mjs tests/lib.test.mjs tests/horde.test.mjs tests/status.test.mjs tests/docs.test.mjs tests/law-audit.test.mjs (the six suites covering the touched files) · **saw:** 289 tests, 287 pass, 1 fail, 1 skipped, 0 cancelled, 0 todo. The one failure (lib.test.mjs, 'no tool but _lib.mjs spells out the shape of a document it reads') is pre-existing and unrelated: confirmed by stashing all six of my changed files and rerunning that one test alone against the unmodified base commit bce058e -- it fails identically there, tripped by a marker string in the test matching unrelated code in land.mjs (return String(command or '')), a file this ticket does not touch. The one skip is a pre-existing root-permission environment skip inside horde.mjs's archive tests, also unrelated.
- **ran:** HORDE_TEST_YG=... node --test tests/tick.test.mjs tests/lib.test.mjs tests/horde.test.mjs tests/status.test.mjs tests/docs.test.mjs tests/law-audit.test.mjs, merger's own run on the merged tip · **saw:** 289 tests, 288 pass, 0 fail, 1 pre-existing unrelated skip

